configuration:
  arguments:
    formNameVar: "myForm"
    runtime: "concord-v2"
flows:
  default:
  - form: ${formNameVar}
  - log: "Hello, ${myForm.name}"
  - log: "Hello, ${context.getVariable(formNameVar).name}"

forms:
  myForm:
    - name: { type: "string" }
  secondForm:
    - date: { type: "string" }
